#  Trunk — Build identity configuration

# --- Kernel identity ---------------------------------------------------------
KERNEL_NAME=trunk
VERSION_MAJOR=0
VERSION_MINOR=4
VERSION_PATCH=5

# --- Architecture ------------------------------------------------------------
ARCH=x86_64

# --- Base directories --------------------------------------------------------
SRC_DIR=src
INCLUDE_DIR=include
SETUP_DIR=src/trunk/mergedcomp/setup
SCRIPTS_DIR=src/trunk/mergedcomp/scripts
CONFIG_DIR=src/trunk/mergedcomp/config
BUILD_DIR=build